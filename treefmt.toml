[formatter.alejandra]
command = "alejandra"
includes = ["*.nix"]

[formatter.prettier]
command = "prettier"
options = ["--tab-width", "2", "--write"]
# TOML excluded: prettier has no TOML parser; taplo (pre-commit hook) covers it.
includes = ["*.json", "*.yaml", "*.yml"]

[formatter.markdownlint]
command = "markdownlint"
options = ["--fix"]
includes = ["*.md"]

# trailing-whitespace-fixer not available in dev shell PATH; would require adding to dev shell
# [formatter.trailing-whitespace]
# command = "trailing-whitespace-fixer"
# includes = ["*"]
# excludes = ["*.patch"]
