mkdir -p "$(direnv_layout_dir)"
use flake .

# sops materializes the derived age key on demand; exporting the key itself
# would expose it in the environment of every process started from here
export SOPS_AGE_KEY_CMD="ssh-to-age -i $HOME/.ssh/id_ed25519 -private-key"
