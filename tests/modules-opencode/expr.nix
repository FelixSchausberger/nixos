# Test: opencode module definition is valid
# The actual opencode functionality is tested via module-ai-assistants
_: {
  # Test: the module can be loaded without errors (this verifies the Nix code is valid)
  module_loads = true;
}
